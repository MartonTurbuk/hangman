import { CallMethodPipe } from './call-method.pipe';

describe('CallMethodPipe', () => {
  it('create an instance', () => {
    const pipe = new CallMethodPipe();
    expect(pipe).toBeTruthy();
  });
});
