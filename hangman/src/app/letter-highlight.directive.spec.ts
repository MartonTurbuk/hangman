import { LetterHighlightDirective } from './letter-highlight.directive';

describe('LetterHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new LetterHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
